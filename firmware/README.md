# Firmware

Embedded firmware lives here. The MCU layer owns time-sensitive and safety-relevant behavior: encoder acquisition, motor commands, watchdog stop, E-stop observation, fault handling, and power telemetry.

Do not make Linux/ROS the only safety layer.
