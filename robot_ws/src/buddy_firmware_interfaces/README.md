# buddy_firmware_interfaces

Defines the contract between ROS 2 and the embedded controllers. This package currently stores placeholder custom messages and protocol docs. Do not generate interfaces from `(_IP)` files until names and fields are finalized.
