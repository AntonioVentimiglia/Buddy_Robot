# buddy_bringup

Top-level launch package for starting Buddy in simulation or hardware. Launch files are intentionally split by subsystem so hardware drivers can be swapped without changing the entire robot graph.
