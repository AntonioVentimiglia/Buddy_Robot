Drop static files here that the site should serve as-is (resume PDF,
favicon, etc.). Everything in this folder is copied to the site root on
build. This README itself is skipped from a rendered page (it's not .md
collected from docs/) but stays as a .md file here for git — harmless to
leave in site/ output; delete this comment if you want it gone from site/.
